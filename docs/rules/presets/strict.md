# typedoc.configs.strict

Strict TypeDoc policy preset for teams that require complete parameter and return-value documentation.

## Rules in this preset

- `Fix` legend:
  - `🔧` = autofixable
  - `💡` = suggestions available
  - `—` = report only

| Rule                                | Fix |
| ----------------------------------- | :-: |
| _Run sync script to generate table_ |  —  |

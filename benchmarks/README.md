# Benchmarks

Benchmark scripts for `eslint-plugin-typedoc` live in this directory.

## Available commands

- `npm run bench`
- `npm run bench:eslint:stats`
- `npm run bench:eslint:timing`

Outputs are written under `coverage/benchmarks/`.
